# Railway Configuration para Frontend
# Este archivo es opcional - Railway detecta Next.js automáticamente

# [build]
# builder = "NIXPACKS"

# [deploy]
# startCommand = "npm start"
# healthcheckPath = "/"
# healthcheckTimeout = 100

# Nota: Railway detectará automáticamente Next.js y usará:
# - Build: npm run build
# - Start: npm start
# Solo necesitas configurar las variables de entorno en Railway Dashboard

